12 个核心 Git 命令速查手册
以下是图中所有命令的完整文本整理，可直接保存为 .txt 文件使用：
1. git init
作用：在当前目录初始化一个全新的 Git 仓库示例命令：
bash
mkdir railsapp
cd railsapp
git init
2. git add
作用：暂存当前目录及子目录下的修改示例命令：
bash
echo "# RAILS APP" > README.md
echo "Demo" >> README.md
git add README.md
3. git commit
作用：将暂存区的修改提交，并附带提交信息示例命令：
bash
git add app.css
git commit -m "Initial commit"
4. git push
作用：将本地修改推送到远程仓库示例命令：
bash
git add .
git commit -m "Update files"
git push origin main
5. git pull
作用：从远程仓库拉取修改，并合并到本地仓库示例命令：
bash
git status
git pull origin main
git log --oneline
6. git remote
作用：添加、查看、重命名远程仓库示例命令：
bash
git remote add origin <url>
git remote -v
git remote rename origin upstr
7. git branch
作用：列出所有分支、创建新分支、确认分支创建成功示例命令：
bash
git branch
git branch feature-login
git branch
8. git fetch
作用：从远程仓库获取最新数据，但不会将数据合并到工作文件中示例命令：
bash
git fetch origin
9. git checkout
作用：切换到指定分支示例命令：
bash
git branch
git checkout feature-login
git branch
10. git merge
作用：将指定分支合并到当前分支（示例中为 main 分支）示例命令：
bash
git checkout main
git merge feature-login
git log --oneline
11. git status
作用：显示仓库状态，包括所有未提交的修改示例命令：
bash
git status
echo "Data Added" >> hello.txt
git status
12. git reset
作用：将当前分支重置到指定的提交版本示例命令：
bash
git log --oneline
git reset --hard <commit-hash>
git status
补充说明
来源：sysxplore.com
所有命令均为 Git 基础核心操作，适合日常开发使用
<url>、<commit-hash> 为占位符，使用时需替换为实际内容
